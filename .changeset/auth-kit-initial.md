---
"@iskra-bun/auth-kit": minor
---

Initial public release. Transport-agnostic authentication kit extracted from web-kit: the `createBetterAuth` config factory, the Drizzle auth schema (Postgres/MySQL/SQLite), and shared auth types — usable outside the HTTP layer.
