---
"@iskra-bun/config-kit": minor
---

Initial public release. Typed, Zod-validated environment/config loading: `loadConfig` returns a deep-frozen config, env coercers (`envBool`, `envNumber`, `envPort`, `envEnum`), and validation errors that never echo secret values.
