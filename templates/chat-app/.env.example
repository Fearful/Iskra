# Puerto del servidor WebSocket
SOCKET_PORT=3001

# Secreto compartido para validar el handshake de autenticacion.
# Los clientes deben construir su token con este mismo valor.
CHAT_AUTH_SECRET=dev-secret

# Adaptador del KV store: "memory" (default) o "redis".
KV_DRIVER=memory
