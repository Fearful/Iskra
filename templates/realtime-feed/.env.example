# Realtime Feed — Variables de entorno
PORT=3000
SOCKET_PORT=3001

# Autores que pueden publicar: <authorId>:author:<clave>, separados por comas.
# Claves de al menos 32 caracteres: openssl rand -hex 32
# Vacio: el feed se lee, pero publicar responde 401.
API_KEYS=
